#include "cmath"

using namespace std;

int is_prime(int number){
    if (number == 1){
        return 0;
    }
    int i;
    for (i = 2; i <= sqrt(number); i++){
        if (number % i == 0)
            return 0;
    }
    return 1;
}
